cc_library(
    name = "influxdb_cpp_headers",
    hdrs = ["influxdb-cpp/influxdb.hpp"],  # Include all header files from your custom protocol
    
    # includes = [
    #     "mavgen",  # Add your_custom_protocol to the include path
    # ],
    # textual_hdrs = [],  # Use this if there are any headers that should not be compiled directly but included
    deps = [],
    visibility = ["//visibility:public"],

)